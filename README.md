# GoEngineKenga

**Мощный игровой движок на Go, способный конкурировать с Unity и Godot!**

## 🚀 Возможности

### 🎮 Ядро движка
- **ECS архитектура** - высокопроизводительная система сущностей и компонентов
- **Сцены и префабы** - иерархическая организация объектов с переиспользуемыми шаблонами
- **Физика** - rigidbody, коллайдеры, гравитация, столкновения
- **Аудио** - 2D/3D звук, пространственное позиционирование
- **UI система** - интерактивные элементы для внутриигровых интерфейсов

### 🎨 Графика и рендеринг
- **WebGPU бэкенд** - современный кроссплатформенный рендеринг
- **Материалы и освещение** - PBR материалы, directional/point/spot lights
- **Текстуры** - поддержка PNG/JPEG, UV mapping
- **glTF 2.0 импорт** - загрузка 3D моделей с материалами и анимациями

### ⚙️ Разработка
- **Визуальный редактор** - Hierarchy, Inspector, Scene View, Content Browser
- **WASM скриптинг** - горячая перезагрузка скриптов без перезапуска
- **Asset pipeline** - автоматический импорт и кеширование ресурсов
- **Система плагинов** - расширяемая архитектура для кастомных компонентов

### 📊 Инструменты
- **Профилировщик** - мониторинг производительности в реальном времени
- **Отладчик** - инспекция состояния мира, логирование, breakpoints
- **CLI инструменты** - импорт ассетов, сборка проектов, скриптинг

## 🏁 Быстрый старт

### Требования
- Go 1.22+
- TinyGo (для WASM скриптинга)

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/GermannM3/GoEngineKenga.git
   cd GoEngineKenga
   ```

2. **Установите зависимости:**
   ```bash
   go mod tidy
   ```

3. **Запустите пример проекта:**
   ```bash
   # Импорт ассетов
   go run ./cmd/kenga import --project samples/hello

   # Запуск игры
   go run ./cmd/kenga run --project samples/hello --scene scenes/main.scene.json --backend ebiten

   # Запуск редактора
   go run ./cmd/kenga-editor
   ```

## 📁 Структура проекта

```
GoEngineKenga/
├── cmd/                    # Исполняемые файлы
│   ├── kenga/             # CLI инструмент
│   └── kenga-editor/      # Редактор
├── engine/                 # Ядро движка
│   ├── ecs/               # Entity-Component-System
│   ├── render/            # Графическая подсистема
│   ├── physics/           # Физический движок
│   ├── audio/             # Аудиосистема
│   ├── ui/                # UI система
│   ├── scene/             # Сцены и префабы
│   ├── asset/             # Управление ресурсами
│   ├── script/            # WASM скриптинг
│   ├── plugin/            # Система плагинов
│   ├── profiler/          # Профилирование
│   └── debug/             # Отладка
├── editor/                 # Визуальный редактор
├── samples/                # Примеры проектов
└── examples/               # Примеры кода
    └── plugins/           # Примеры плагинов
```

## 🎯 Компоненты ECS

### Базовые компоненты
- **Transform** - позиция, вращение, масштаб
- **Camera** - параметры камеры (FOV, near/far planes)
- **MeshRenderer** - рендеринг 3D моделей
- **Light** - источники освещения

### Физика
- **Rigidbody** - физическое тело с массой и скоростью
- **Collider** - формы коллизий (box, sphere, capsule)

### Аудио
- **AudioSource** - источники звука с 3D позиционированием

### UI
- **UICanvas** - поверхность для рисования интерфейса

## 🔧 Скриптинг

Используйте TinyGo для компиляции Go кода в WASM:

```go
// scripts/game/main.go
package main

import "unsafe"

//go:wasmimport env debugLog
func debugLog(ptr uint32, l uint32)

func debug(msg string) {
    ptr := uintptr(unsafe.Pointer(&[]byte(msg)[0]))
    debugLog(uint32(ptr), uint32(len(msg)))
}

//export Update
func Update(dtMillis int32) {
    debug("Hello from WASM!")
}
```

**Сборка скрипта:**
```bash
go run ./cmd/kenga script build --project samples/hello
```

## 🛠️ Система плагинов

Расширяйте движок без изменения основного кода:

```go
type MyPlugin struct{}

func (p MyPlugin) Init(manager *plugin.Manager) error {
    // Инициализация
    return nil
}

func (p MyPlugin) Update(deltaTime float32) error {
    // Логика плагина
    return nil
}

func (p MyPlugin) CreateSystem(world *ecs.World) plugin.System {
    return &MySystem{}
}
```

## 🎮 Примеры использования

### Создание сцены с физикой

```json
{
  "name": "PhysicsDemo",
  "entities": [
    {
      "name": "FallingCube",
      "transform": {
        "position": {"x": 0, "y": 5, "z": 0}
      },
      "meshRenderer": {
        "meshAssetId": "cube_mesh_id",
        "materialAssetId": "cube_material_id"
      },
      "rigidbody": {
        "mass": 1.0,
        "useGravity": true
      },
      "collider": {
        "type": "box",
        "size": {"x": 1, "y": 1, "z": 1}
      }
    }
  ]
}
```

### Работа с префабами

```go
// Создание префаба
prefab := scene.CreatePrefabFromEntities(world, entityIDs, "Enemy")

// Сохранение префаба
scene.SavePrefab("prefabs/enemy.prefab", prefab)

// Инстанцирование
position := emath.V3(10, 0, 5)
entityID := prefab.Instantiate(world, position, overrides)
```

## 📈 Производительность

- **Оптимизированная ECS** - быстрая итерация компонентов
- **WebGPU рендеринг** - аппаратное ускорение графики
- **WASM скриптинг** - изолированная среда выполнения
- **Память эффективно** - автоматическое управление ресурсами

## 🔄 Сравнение с Unity

| Функция | Unity | GoEngineKenga |
|---------|-------|---------------|
| Язык | C# | Go |
| ECS | ✅ | ✅ |
| Физика | ✅ | ✅ |
| Аудио | ✅ | ✅ |
| UI | ✅ | ✅ |
| Скриптинг | C# | WASM (Go) |
| Плагины | ✅ | ✅ |
| Кроссплатформа | ✅ | ✅ |
| Размер | Большой | Маленький |
| Скорость сборки | Средняя | Быстрая |

## 🤝 Вклад в проект

Приветствуются pull requests! Основные направления развития:

- **WebGPU рендеринг** - полноценная 3D графика
- **Расширенная физика** - мягкие тела, ragdoll
- **UI редактор** - улучшение визуального редактора
- **Сетевой мультиплеер** - онлайн игры
- **Мобильные платформы** - Android/iOS поддержка

## 📄 Лицензия

MIT License - свободное использование для любых целей.

---

**GoEngineKenga** — [https://github.com/GermannM3/GoEngineKenga](https://github.com/GermannM3/GoEngineKenga) 🚀🎮
